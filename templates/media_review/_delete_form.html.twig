<form method="post" action="{{ path('app_media_review_delete', {'id': media_review.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ media_review.id) }}">
    <button class="btn">Delete</button>
</form>
